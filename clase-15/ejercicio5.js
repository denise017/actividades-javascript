const prompt = require("prompt-sync")();

// lista de colores
let colores = ["rojo", "verde", "azul", "amarillo"];

// buscar indice del color azul
let indice = colores.indexOf("azul");

// mostrar resultado
console.log("indice de azul: " + indice);
