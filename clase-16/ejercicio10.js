const prompt = require("prompt-sync")();
// ejercicio 10 construir una frase a partir de palabras

let palabras = ["Me", "gusta", "aprender", "JavaScript"];

// unir palabras en una frase
let fraseFinal = palabras.join(" ");

console.log(fraseFinal);