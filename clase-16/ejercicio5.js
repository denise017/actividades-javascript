const prompt = require("prompt-sync")();
// ejercicio 5 recorrer una lista e imprimir cada elemento

let frutas = ["manzana", "banana", "cereza"];

// mostrar cada fruta
frutas.forEach(function(item) {
  console.log(item);
});